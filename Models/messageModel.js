const mongoose = require ('mongoose');

const MessageSchema = new mongoose.Schema ({
 SS

    date: {
        type: Date,
        required: true

    date: {
        type: Date,
        rerquired: true,
 develop
    },
    text: {
        type: String,
        required: true
SS

    },

    owner: Object.id,

    room: Object.id
})

    },
    owner: Object.id,


});
 develop
